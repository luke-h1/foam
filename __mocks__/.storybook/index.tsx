import React from 'react';
import { View } from 'react-native';

// Mock Storybook UI component for Jest tests
const StorybookUIRoot = () => <View testID="storybook-mock" />;

export default StorybookUIRoot;
