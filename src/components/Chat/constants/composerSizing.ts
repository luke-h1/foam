import { theme } from '@app/styles/themes';

export const COMPOSER_CONTROL_SIZE = 48;
export const COMPOSER_CONTROL_RADIUS = COMPOSER_CONTROL_SIZE / 2;
export const COMPOSER_INPUT_MIN_HEIGHT = COMPOSER_CONTROL_SIZE;
export const COMPOSER_ROW_GAP = theme.space8;
