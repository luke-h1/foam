/**
 * Leading/trailing inset an `@expo/ui` `Form` row gives its content (section margin + row inset); a screen-width `RNHostView` needs this padding to line up with native rows.
 */
export const SWIFTUI_ROW_CONTENT_INSET = 32;
