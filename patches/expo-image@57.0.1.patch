diff --git a/ios/AnimatedImage.swift b/ios/AnimatedImage.swift
index 350c9e4d90680baf8370e49b3529f6e45781244e..878e8b2b81f29cedf9bbeaee46207a916b86be61 100644
--- a/ios/AnimatedImage.swift
+++ b/ios/AnimatedImage.swift
@@ -8,16 +8,32 @@ internal import SDWebImage
 final class AnimatedImage: SDAnimatedImage {
   var frames: [SDImageFrame]?
 
+  var hasMultipleFrames: Bool {
+    return animatedImageFrameCount > 1
+  }
+
   // MARK: - UIImage
 
   override var images: [UIImage]? {
-    preloadAllFrames()
-    return frames?.map({ $0.image })
+    return nil
   }
 
   override var duration: TimeInterval {
-    preloadAllFrames()
-    return frames?.reduce(0, { $0 + $1.duration }) ?? 0.0
+    return (0..<animatedImageFrameCount).reduce(0.0) { total, index in
+      return total + animatedImageDuration(at: index)
+    }
+  }
+
+  // MARK: - Shared animation
+
+  /**
+   `preloadAllFrames()` is overridden below to fill `frames`, which deliberately
+   skips SDWebImage's own frame store. `SharedAnimationDriver` needs that store:
+   it is what turns `seekToFrameAtIndex:` into an array lookup instead of a fresh
+   WebP/AVIF decode on whatever thread asked for the frame.
+   */
+  func preloadFramesForSharedPlayback() {
+    super.preloadAllFrames()
   }
 
   // MARK: - SDAnimatedImage
diff --git a/ios/Coders/WebPCoder.swift b/ios/Coders/WebPCoder.swift
index 785effb851802a339f159fb1189b092b84a949b7..804672e98c2bf367a7dd1ebfeedc0b1085d1681f 100644
--- a/ios/Coders/WebPCoder.swift
+++ b/ios/Coders/WebPCoder.swift
@@ -40,6 +40,16 @@ internal final class WebPCoder: NSObject, SDAnimatedImageCoder {
   }
 
   func decodedImage(with data: Data?, options: [SDImageCoderOption: Any]? = nil) -> UIImage? {
+    // The shared coder instance carries the default `useAppleWebpCodec` rather than the
+    // caller's, so honour the per-decode option here too. `SDImageAWebPCoder` builds
+    // multi-frame animated WebP as a plain `UIImage` of lazy `CGImage`s, which UIKit
+    // renders through a `CAKeyframeAnimation` and decodes on the main thread inside the
+    // CoreAnimation commit - a multi-second hang for a screenful of animated emotes.
+    if self.instantiatedCoder == nil,
+      let useAppleWebpCodec = options?[imageCoderOptionUseAppleWebpCodec] as? Bool {
+      let coder: SDAnimatedImageCoder = useAppleWebpCodec ? SDImageAWebPCoder.shared : SDImageWebPCoder.shared
+      return coder.decodedImage(with: data, options: options)
+    }
     return self.coder.decodedImage(with: data, options: options)
   }
 
diff --git a/ios/Image.swift b/ios/Image.swift
index 5c92efa2ec1ca32fc4c49b0e30f3f809259508f3..2c2dd898a9b5721ac16738640a06d80ebf259190 100644
--- a/ios/Image.swift
+++ b/ios/Image.swift
@@ -8,6 +8,9 @@ internal final class Image: SharedRef<UIImage> {
   }
 
   var isAnimated: Bool {
+    if let animatedImage = ref as? AnimatedImage {
+      return animatedImage.hasMultipleFrames
+    }
     return !(ref.images?.isEmpty ?? true)
   }
 
diff --git a/ios/ImageLoader.swift b/ios/ImageLoader.swift
index de6e2c368e45bedd01e563cf254c006190aca233..cd29dcd08631da9c703175756882af0127164f35 100644
--- a/ios/ImageLoader.swift
+++ b/ios/ImageLoader.swift
@@ -14,7 +14,10 @@ internal final class ImageLoader {
   func load(_ source: ImageSource, options: ImageLoadOptions) async throws -> UIImage {
     // This loader uses only the disk cache. We may want to give more control on this, but the memory cache
     // doesn't make much sense for shared refs as they're kept in memory as long as their JS objects.
-    var context = createSDWebImageContext(forSource: source, cachePolicy: .disk)
+    // `useAppleWebpCodec` defaults to true, but images loaded here become long-lived
+    // `ImageRef`s that are rendered directly, so the Apple codec's lazy frames would be
+    // decoded on the main thread at render time. Use libwebp for the ref path.
+    var context = createSDWebImageContext(forSource: source, cachePolicy: .disk, useAppleWebpCodec: false)
 
     if let maxSize = options.getMaxSize() {
       // Note that setting the thumbnail size rasterizes vector images into a bitmap.
diff --git a/ios/ImageModule.swift b/ios/ImageModule.swift
index 974047c904a8776f542266ededdc3db3e7149bf6..7bc7a91a8c3b57361800a7eda046114f0612a0b5 100644
--- a/ios/ImageModule.swift
+++ b/ios/ImageModule.swift
@@ -28,6 +28,11 @@ public final class ImageModule: Module {
       Prop("source") { (view: ImageView, sources: Either<[ImageSource], SharedRef<UIImage>>?) in
         if let imageRef: SharedRef<UIImage> = sources?.get() {
           // Unset an array of traditional sources and just render the image ref right away.
+          // Cancel any in-flight uri load first: `reload()` early-returns before its own
+          // cancel on the ref path, so an abandoned SDWebImage operation would otherwise
+          // keep running behind the displayed ref and deliver a stale onError/onLoad for
+          // a source the view no longer shows.
+          view.cancelPendingOperation()
           view.sources = nil
           view.renderSourceImage(imageRef.ref)
         } else {
@@ -136,6 +141,8 @@ public final class ImageModule: Module {
       AsyncFunction("startAnimating") { (view: ImageView) in
         if view.isSFSymbolSource {
           view.startSymbolAnimation()
+        } else if view.hasSharedAnimation {
+          view.startSharedAnimation()
         } else {
           view.sdImageView.startAnimating()
         }
@@ -144,6 +151,8 @@ public final class ImageModule: Module {
       AsyncFunction("stopAnimating") { (view: ImageView) in
         if view.isSFSymbolSource {
           view.stopSymbolAnimation()
+        } else if view.hasSharedAnimation {
+          view.stopSharedAnimation()
         } else {
           view.sdImageView.stopAnimating()
         }
@@ -170,6 +179,21 @@ public final class ImageModule: Module {
       ImageModule.configureCache(config: config)
     }
 
+    /**
+     Phase-locks every animated image to one shared clock, so identical images
+     start and end on the same frame no matter when each view mounted. Exposed
+     as a runtime switch so the two modes can be compared on the same session.
+     */
+    Function("setSharedAnimationEnabled") { (enabled: Bool) in
+      // Module-level functions aren't main-isolated, and the driver owns a
+      // display link and a view table.
+      DispatchQueue.main.async {
+        MainActor.assumeIsolated {
+          SharedAnimationDriver.shared.isEnabled = enabled
+        }
+      }
+    }
+
     AsyncFunction("prefetch") { (urls: [URL], cachePolicy: ImageCachePolicy, headersMap: [String: String]?, promise: Promise) in
       var context = SDWebImageContext()
       let sdCacheType = cachePolicy.toSdCacheType().rawValue
diff --git a/ios/ImageView.swift b/ios/ImageView.swift
index cccedd7adf7328b18bb61333c3fe2473c79e72cd..953d525a576926d96f9b849d1b523766e7c98b80 100644
--- a/ios/ImageView.swift
+++ b/ios/ImageView.swift
@@ -66,12 +66,23 @@ public final class ImageView: ExpoView {
   var recyclingKey: String? {
     didSet {
       if oldValue != nil && recyclingKey != oldValue {
+        leaveSharedAnimation()
         sdImageView.image = nil
         placeholderImage = nil
       }
     }
   }
 
+  /**
+   The animated image `SharedAnimationDriver` is stepping this view through, and
+   its frame schedule. Both nil whenever the view is on SDWebImage's own
+   per-view player, which is every non-animated image and any animation the
+   driver declined to take.
+   */
+  var sharedAnimationImage: SDAnimatedImage?
+
+  private var sharedAnimationTimeline: AnimationTimeline?
+
   var autoplay: Bool = true
 
   var sfEffect: [SFSymbolEffect]?
@@ -150,6 +161,22 @@ public final class ImageView: ExpoView {
     if lockResource && !force {
       return
     }
+    // A directly-set image ref (`source` is a SharedRef, so `sources == nil`) is not
+    // reloadable. If the view was emptied after the ref was applied — e.g. a `recyclingKey`
+    // change whose `sdImageView.image = nil` ran after the source prop in the same prop batch —
+    // reload would fall through to the placeholder and the image would stay permanently blank.
+    // Re-assert the retained ref here so the ref path self-heals like `sources`-based loads do.
+    if sources == nil, let sourceImage {
+      // The source prop handler renders the ref mid-batch, so `setImage` may have
+      // read prop defaults (`contentFit` is `.cover`) before the same batch's real
+      // values were applied. This hook runs after the whole batch - re-assert the
+      // final fit so the first render can't lock in a default.
+      sdImageView.contentMode = contentFit.toContentMode()
+      if isViewEmpty {
+        renderSourceImage(sourceImage)
+      }
+      return
+    }
     if isViewEmpty {
       if placeholderImage != nil {
         displayPlaceholderIfNecessary()
@@ -526,10 +553,15 @@ public final class ImageView: ExpoView {
   private func setImage(_ image: UIImage?, contentFit: ContentFit, isPlaceholder: Bool) {
     sdImageView.contentMode = contentFit.toContentMode()
 
+    // Resolved before the image is assigned, because assigning it is what makes
+    // `SDAnimatedImageView` consult `autoPlayAnimatedImage` and start its own
+    // player - which a driver-stepped image must never do.
+    let sharedAnimation = drivableAnimatedImage(image, isPlaceholder: isPlaceholder)
+
     if isPlaceholder {
       sdImageView.autoPlayAnimatedImage = true
     } else {
-      sdImageView.autoPlayAnimatedImage = autoplay
+      sdImageView.autoPlayAnimatedImage = sharedAnimation == nil && autoplay
     }
 
     // Remove any existing symbol effects before setting new image
@@ -565,6 +597,8 @@ public final class ImageView: ExpoView {
       }
     }
 
+    updateSharedAnimation(with: sharedAnimation)
+
     if !isPlaceholder {
       onDisplay()
     }
@@ -576,6 +610,97 @@ public final class ImageView: ExpoView {
 #endif
   }
 
+  // MARK: - Shared animation
+
+  /**
+   Whether this view currently has an image on the shared clock. `startAnimating`
+   and `stopAnimating` route around `SDAnimatedImageView` when it does.
+   */
+  var hasSharedAnimation: Bool {
+    return sharedAnimationTimeline != nil
+  }
+
+  private func drivableAnimatedImage(_ image: UIImage?, isPlaceholder: Bool) -> SDAnimatedImage? {
+    // Placeholders are short-lived and tint/SF-symbol rendering replaces the
+    // displayed image outright, so neither is worth phase-locking.
+    guard !isPlaceholder,
+      !isSFSymbolSource,
+      imageTintColor == nil,
+      let animatedImage = image as? SDAnimatedImage,
+      SharedAnimationDriver.shared.canDrive(animatedImage) else {
+      return nil
+    }
+    return animatedImage
+  }
+
+  private func updateSharedAnimation(with image: SDAnimatedImage?) {
+    guard let image, let timeline = SharedAnimationDriver.shared.timeline(for: image) else {
+      leaveSharedAnimation()
+      return
+    }
+
+    sharedAnimationImage = image
+    sharedAnimationTimeline = timeline
+
+    if autoplay {
+      startSharedAnimation()
+    } else {
+      SharedAnimationDriver.shared.unregister(self)
+    }
+  }
+
+  func startSharedAnimation() {
+    guard let sharedAnimationImage else {
+      return
+    }
+    if !SharedAnimationDriver.shared.register(self, image: sharedAnimationImage) {
+      // The driver has no room to hold this image's frames, so it stays on its
+      // own player rather than paying a decode per seek.
+      leaveSharedAnimation()
+    }
+  }
+
+  func stopSharedAnimation() {
+    // Leaves the current frame on screen, matching `SDAnimatedImageView`'s
+    // pause-on-stop behaviour for a view that keeps its image.
+    SharedAnimationDriver.shared.unregister(self)
+  }
+
+  /**
+   Hands the view back to SDWebImage's own player. Called when the image is
+   replaced or recycled, and when the driver is turned off wholesale.
+   */
+  func leaveSharedAnimation() {
+    guard sharedAnimationTimeline != nil else {
+      return
+    }
+    SharedAnimationDriver.shared.unregister(self)
+    sharedAnimationImage = nil
+    sharedAnimationTimeline = nil
+    sdImageView.autoPlayAnimatedImage = autoplay
+    if autoplay {
+      sdImageView.startAnimating()
+    }
+  }
+
+  /**
+   Steps the view to the frame the shared clock says every view of this image
+   should be showing. Seeking renders through the player's frame handler without
+   the player ever playing, so no per-view display link is involved.
+   */
+  func renderSharedAnimationFrame(elapsed: TimeInterval) {
+    guard let sharedAnimationTimeline,
+      let sharedAnimationImage,
+      SharedAnimationDriver.shared.isReady(sharedAnimationImage),
+      let player = sdImageView.player else {
+      return
+    }
+    let frameIndex = sharedAnimationTimeline.frameIndex(atElapsed: elapsed)
+    if player.currentFrameIndex != frameIndex {
+      player.seekToFrame(at: frameIndex, loopCount: 0)
+    }
+  }
+
   // MARK: - Symbol Effects
 
   @available(iOS 17.0, tvOS 17.0, *)
diff --git a/ios/SharedAnimationDriver.swift b/ios/SharedAnimationDriver.swift
new file mode 100644
index 0000000000000000000000000000000000000000..5ad88815a9a8d6caab8bfb6b4d3401d6c743d7ec
--- /dev/null
+++ b/ios/SharedAnimationDriver.swift
@@ -0,0 +1,397 @@
+// Copyright 2022-present 650 Industries. All rights reserved.
+
+internal import SDWebImage
+import QuartzCore
+import UIKit
+
+/**
+ The wall-clock offset at which each frame of an animated image ends, plus the
+ total loop length. Built once per image so mapping an elapsed time onto a frame
+ index is a binary search rather than a walk over the frame list every tick.
+ */
+internal struct AnimationTimeline {
+  private let frameEnds: [TimeInterval]
+  let duration: TimeInterval
+
+  init?(image: SDAnimatedImage) {
+    let frameCount = image.animatedImageFrameCount
+    guard frameCount > 1 else {
+      return nil
+    }
+
+    var ends = [TimeInterval]()
+    ends.reserveCapacity(Int(frameCount))
+    var total: TimeInterval = 0
+
+    for index in 0..<frameCount {
+      let frameDuration = image.animatedImageDuration(at: index)
+      // Mirrors SDWebImage's own guard: encoders routinely emit 0 or 10ms frame
+      // delays that browsers historically rewrote to 100ms.
+      total += frameDuration < 0.011 ? 0.1 : frameDuration
+      ends.append(total)
+    }
+
+    guard total > 0 else {
+      return nil
+    }
+    frameEnds = ends
+    duration = total
+  }
+
+  /**
+   The frame showing at `elapsed` seconds after the driver's epoch. Loop position
+   is a pure function of the clock, so two views of the same image agree no matter
+   when either of them mounted, and images of different lengths never drift.
+   */
+  func frameIndex(atElapsed elapsed: TimeInterval) -> UInt {
+    var offset = elapsed.truncatingRemainder(dividingBy: duration)
+    if offset < 0 {
+      offset += duration
+    }
+
+    var low = 0
+    var high = frameEnds.count - 1
+    while low < high {
+      let mid = (low + high) / 2
+      if offset < frameEnds[mid] {
+        high = mid
+      } else {
+        low = mid + 1
+      }
+    }
+    return UInt(low)
+  }
+}
+
+/**
+ `UIImage` isn't `Sendable`, but the frame store `preloadAllFrames()` fills is
+ lock-guarded inside SDWebImage, so handing an image to a background queue purely
+ to fill that store is safe.
+ */
+private final class ImageBox: @unchecked Sendable {
+  let image: SDAnimatedImage
+
+  init(_ image: SDAnimatedImage) {
+    self.image = image
+  }
+}
+
+/**
+ Drives every animated image view from one `CADisplayLink` and one global epoch.
+
+ SDWebImage gives each `SDAnimatedImageView` its own display link, started when
+ that view first shows its image. Two views of the same emote therefore animate
+ out of phase by however long apart they mounted - in chat, where the same emote
+ lands on rows seconds apart, that reads as noise. Here the per-view players are
+ parked (`autoPlayAnimatedImage = false`) and the driver seeks each registered
+ view to `f(now - epoch)` instead, so identical images are always on the same
+ frame and the process holds one display link rather than N.
+
+ Seeking bypasses the player's frame buffer and asks the coder for the frame
+ directly, so a synced image has all of its frames preloaded into SDWebImage's
+ frame store first. That store lives on the image, and foam's emote cache hands
+ the same `ImageRef` to every row showing a given emote, so the decoded frames
+ are shared by every view of that emote instead of buffered once per player.
+ */
+@MainActor
+internal final class SharedAnimationDriver: NSObject {
+  static let shared = SharedAnimationDriver()
+
+  /**
+   Frames are pushed at most this often. Emotes top out around 25fps and the
+   point of the shared clock is to cut per-view work, so sampling at the
+   display's full rate would only burn power to render repeat frames.
+   */
+  private static let framesPerSecond: Float = 30
+
+  /**
+   Preload budgets, in bytes of decoded frames. Emote-sized animations are the
+   target; a large animation would blow through memory frame-first, so anything
+   over the per-image cap keeps its own player and stays unsynced. The per-image
+   cap must cover a long emote (a 200+ frame 4x is ~20MB decoded) - a stock
+   playing `SDAnimatedImagePlayer` with the default dynamic buffer holds the
+   same order of decoded frames per *view*, so the shared per-image store stays
+   the cheaper option even at this size.
+   */
+  private static let maxPreloadBytesPerImage = 24 * 1024 * 1024
+  private static let maxPreloadBytesTotal = 96 * 1024 * 1024
+
+  /**
+   Bookkeeping for one animated image whose frames are held decoded.
+   */
+  private final class Preload {
+    weak var image: SDAnimatedImage?
+    let bytes: Int
+    var lastUsed: CFTimeInterval
+
+    init(image: SDAnimatedImage, bytes: Int, lastUsed: CFTimeInterval) {
+      self.image = image
+      self.bytes = bytes
+      self.lastUsed = lastUsed
+    }
+  }
+
+  /**
+   A frame schedule, cached against the image it was built from. Building one
+   walks every frame, and foam's emote cache hands the same image to every row
+   showing that emote, so it is worth doing once rather than per row.
+   */
+  private final class CachedTimeline {
+    weak var image: SDAnimatedImage?
+    let timeline: AnimationTimeline
+
+    init(image: SDAnimatedImage, timeline: AnimationTimeline) {
+      self.image = image
+      self.timeline = timeline
+    }
+  }
+
+  /**
+   Turns the shared clock off for images displayed from here on. Existing synced
+   views are handed back to their own players immediately.
+   */
+  var isEnabled = true {
+    didSet {
+      if !isEnabled {
+        releaseAll()
+      }
+    }
+  }
+
+  /**
+   Stale entries are only pruned under budget pressure, so cap the timeline cache
+   independently - it holds no pixels and would otherwise grow with every emote
+   the session has ever shown.
+   */
+  private static let maxCachedTimelines = 512
+
+  private let epoch = CACurrentMediaTime()
+  private var displayLink: CADisplayLink?
+  private let views = NSHashTable<ImageView>.weakObjects()
+  private var preloads = [ObjectIdentifier: Preload]()
+  private var timelines = [ObjectIdentifier: CachedTimeline]()
+  private var preloadedBytes = 0
+
+  // MARK: - Registration
+
+  /**
+   Whether `image` is the right shape for the shared clock. Answered before the
+   image is handed to the view, because assigning it is what makes the view
+   decide whether to start its own player.
+   */
+  func canDrive(_ image: SDAnimatedImage) -> Bool {
+    guard isEnabled, image.animatedImageFrameCount > 1 else {
+      return false
+    }
+    let bytes = decodedByteEstimate(of: image)
+    return bytes > 0 && bytes <= Self.maxPreloadBytesPerImage
+  }
+
+  /**
+   The frame schedule for `image`, or nil if it has no usable one. Cached against
+   the image so rows showing the same emote share the walk.
+   */
+  func timeline(for image: SDAnimatedImage) -> AnimationTimeline? {
+    let key = ObjectIdentifier(image)
+    if let cached = timelines[key], cached.image === image {
+      return cached.timeline
+    }
+    guard let timeline = AnimationTimeline(image: image) else {
+      return nil
+    }
+    if timelines.count >= Self.maxCachedTimelines {
+      timelines = timelines.filter { $0.value.image != nil }
+    }
+    timelines[key] = CachedTimeline(image: image, timeline: timeline)
+    return timeline
+  }
+
+  /**
+   Takes over stepping `view`. Returns false when the frame budget can't cover
+   this image, in which case the caller must leave the view on its own player -
+   driving it without preloaded frames would decode on the main thread every tick.
+   */
+  func register(_ view: ImageView, image: SDAnimatedImage) -> Bool {
+    guard isEnabled, retainPreload(of: image) else {
+      return false
+    }
+    views.add(view)
+    startDisplayLinkIfNeeded()
+    return true
+  }
+
+  func unregister(_ view: ImageView) {
+    views.remove(view)
+    stopDisplayLinkIfIdle()
+  }
+
+  /**
+   Whether `image`'s frames are decoded and resident. Views hold off seeking
+   until they are: a seek asks the coder for the frame directly, which on the
+   main thread would both decode and block on the lock the preload is holding.
+   */
+  func isReady(_ image: SDAnimatedImage) -> Bool {
+    return image.isAllFramesLoaded
+  }
+
+  // MARK: - Clock
+
+  private func startDisplayLinkIfNeeded() {
+    guard displayLink == nil else {
+      return
+    }
+    let link = CADisplayLink(target: self, selector: #selector(step))
+    link.preferredFrameRateRange = CAFrameRateRange(
+      minimum: Self.framesPerSecond / 2,
+      maximum: Self.framesPerSecond,
+      preferred: Self.framesPerSecond
+    )
+    link.add(to: .main, forMode: .common)
+    displayLink = link
+  }
+
+  private func stopDisplayLinkIfIdle() {
+    guard views.allObjects.isEmpty else {
+      return
+    }
+    displayLink?.invalidate()
+    displayLink = nil
+  }
+
+  @objc
+  private func step() {
+    let elapsed = CACurrentMediaTime() - epoch
+    let registered = views.allObjects
+
+    if registered.isEmpty {
+      displayLink?.invalidate()
+      displayLink = nil
+      return
+    }
+
+    for view in registered {
+      view.renderSharedAnimationFrame(elapsed: elapsed)
+    }
+  }
+
+  // MARK: - Preloading
+
+  private func retainPreload(of image: SDAnimatedImage) -> Bool {
+    let key = ObjectIdentifier(image)
+    let now = CACurrentMediaTime()
+
+    if let existing = preloads[key] {
+      // A dealloc'd image can leave its address to a new one, so identity has to
+      // be confirmed - otherwise this would report frames resident that aren't.
+      if existing.image === image {
+        existing.lastUsed = now
+        return true
+      }
+      preloads.removeValue(forKey: key)
+      preloadedBytes -= existing.bytes
+    }
+
+    let bytes = decodedByteEstimate(of: image)
+    guard bytes > 0, bytes <= Self.maxPreloadBytesPerImage, makeRoom(for: bytes) else {
+      return false
+    }
+
+    preloads[key] = Preload(image: image, bytes: bytes, lastUsed: now)
+    preloadedBytes += bytes
+
+    // Decoding every frame is the expensive part, and it is the same work the
+    // per-view players would each do lazily. Do it once, off the main thread;
+    // registered views hold off seeking until `isAllFramesLoaded` flips.
+    let box = ImageBox(image)
+    Task { @MainActor in
+      await Self.decodeAllFrames(box)
+      // A coder that won't hand back frames would otherwise leave every view of
+      // this image parked on frame 0 for good, with nothing to retry it.
+      if !box.image.isAllFramesLoaded {
+        self.releaseViews(showing: box.image)
+      }
+    }
+    return true
+  }
+
+  private nonisolated static func decodeAllFrames(_ box: ImageBox) async {
+    await withCheckedContinuation { continuation in
+      DispatchQueue.global(qos: .userInitiated).async {
+        if let animatedImage = box.image as? AnimatedImage {
+          animatedImage.preloadFramesForSharedPlayback()
+        } else {
+          box.image.preloadAllFrames()
+        }
+        continuation.resume()
+      }
+    }
+  }
+
+  /**
+   Hands every view showing `image` back to its own player and drops the frame
+   budget the image was holding.
+   */
+  private func releaseViews(showing image: SDAnimatedImage) {
+    if let preload = preloads.removeValue(forKey: ObjectIdentifier(image)) {
+      preloadedBytes -= preload.bytes
+    }
+    for view in views.allObjects where view.sharedAnimationImage === image {
+      view.leaveSharedAnimation()
+    }
+  }
+
+  /**
+   Evicts frames for images no registered view is showing, oldest first, until
+   `bytes` fits under the total budget.
+   */
+  private func makeRoom(for bytes: Int) -> Bool {
+    if preloadedBytes + bytes <= Self.maxPreloadBytesTotal {
+      return true
+    }
+
+    for (key, preload) in preloads where preload.image == nil {
+      preloads.removeValue(forKey: key)
+      preloadedBytes -= preload.bytes
+    }
+
+    let inUse = Set(views.allObjects.compactMap { $0.sharedAnimationImage.map(ObjectIdentifier.init) })
+    let evictable = preloads
+      .filter { !inUse.contains($0.key) }
+      .sorted { $0.value.lastUsed < $1.value.lastUsed }
+
+    for (key, preload) in evictable {
+      if preloadedBytes + bytes <= Self.maxPreloadBytesTotal {
+        break
+      }
+      preload.image?.unloadAllFrames()
+      preloads.removeValue(forKey: key)
+      preloadedBytes -= preload.bytes
+    }
+
+    return preloadedBytes + bytes <= Self.maxPreloadBytesTotal
+  }
+
+  private func releaseAll() {
+    for view in views.allObjects {
+      view.leaveSharedAnimation()
+    }
+    views.removeAllObjects()
+    for preload in preloads.values {
+      preload.image?.unloadAllFrames()
+    }
+    preloads.removeAll()
+    preloadedBytes = 0
+    displayLink?.invalidate()
+    displayLink = nil
+  }
+
+  private func decodedByteEstimate(of image: SDAnimatedImage) -> Int {
+    let scale = max(image.scale, 1)
+    let width = Int((image.size.width * scale).rounded(.up))
+    let height = Int((image.size.height * scale).rounded(.up))
+    guard width > 0, height > 0 else {
+      return 0
+    }
+    return width * height * 4 * Int(image.animatedImageFrameCount)
+  }
+}
