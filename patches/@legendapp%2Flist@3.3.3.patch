diff --git a/react-native.js b/react-native.js
index 229f09a8230ab24079d471f65d28d36908f0f2ea..86dd170a964f453db7554349061418320b3fbe9d 100644
--- a/react-native.js
+++ b/react-native.js
@@ -3932,12 +3932,17 @@ function setDidLayout(ctx) {
 // src/core/calculateItemsInView.ts
 var RENDER_RANGE_PROJECTION_FULL_VELOCITY = 4;
 var RENDER_RANGE_PROJECTION_SETTLE_DELAY = 100;
+// PATCH(foam): most of the trailing buffer the projection may consume.
+var RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT = 0.5;
 function getProjectedBufferAdjustment(scrollVelocity, trailingBuffer) {
   if (trailingBuffer <= 0) {
     return 0;
   }
   const velocityProgress = Math.min(1, Math.abs(scrollVelocity) / RENDER_RANGE_PROJECTION_FULL_VELOCITY);
-  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress;
+  // PATCH(foam): the caller adds this to BOTH buffered edges, so at full velocity the
+  // trailing edge lands on the viewport and rows behind the scroll leave the render
+  // range until the settle - chat scrollback shows holes where they should be (#858).
+  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress * RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT;
 }
 function scheduleRenderRangeProjectionSettle(ctx) {
   const state = ctx.state;
@@ -3951,7 +3956,10 @@ function scheduleRenderRangeProjectionSettle(ctx) {
     state.timeoutRenderRangeProjectionSettle = void 0;
     state.timeouts.delete(timeout);
     state.scrollHistory.length = 0;
-    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state);
+    // PATCH(foam): without this flag updateItemPositions starts at
+    // state.startBuffered, so a row above it keeps the stale position it got from
+    // the size estimate and leaves a hole below itself (#858).
+    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state, { forceFullItemPositions: true });
   }, RENDER_RANGE_PROJECTION_SETTLE_DELAY);
   state.timeoutRenderRangeProjectionSettle = timeout;
   state.timeouts.add(timeout);
diff --git a/react-native.mjs b/react-native.mjs
index c2e0f3823f524e31e2105a5fbda76be78c926226..653bd3269cd63cbdb4a412f29d2f244638304554 100644
--- a/react-native.mjs
+++ b/react-native.mjs
@@ -3911,12 +3911,17 @@ function setDidLayout(ctx) {
 // src/core/calculateItemsInView.ts
 var RENDER_RANGE_PROJECTION_FULL_VELOCITY = 4;
 var RENDER_RANGE_PROJECTION_SETTLE_DELAY = 100;
+// PATCH(foam): most of the trailing buffer the projection may consume.
+var RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT = 0.5;
 function getProjectedBufferAdjustment(scrollVelocity, trailingBuffer) {
   if (trailingBuffer <= 0) {
     return 0;
   }
   const velocityProgress = Math.min(1, Math.abs(scrollVelocity) / RENDER_RANGE_PROJECTION_FULL_VELOCITY);
-  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress;
+  // PATCH(foam): the caller adds this to BOTH buffered edges, so at full velocity the
+  // trailing edge lands on the viewport and rows behind the scroll leave the render
+  // range until the settle - chat scrollback shows holes where they should be (#858).
+  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress * RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT;
 }
 function scheduleRenderRangeProjectionSettle(ctx) {
   const state = ctx.state;
@@ -3930,7 +3935,10 @@ function scheduleRenderRangeProjectionSettle(ctx) {
     state.timeoutRenderRangeProjectionSettle = void 0;
     state.timeouts.delete(timeout);
     state.scrollHistory.length = 0;
-    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state);
+    // PATCH(foam): without this flag updateItemPositions starts at
+    // state.startBuffered, so a row above it keeps the stale position it got from
+    // the size estimate and leaves a hole below itself (#858).
+    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state, { forceFullItemPositions: true });
   }, RENDER_RANGE_PROJECTION_SETTLE_DELAY);
   state.timeoutRenderRangeProjectionSettle = timeout;
   state.timeouts.add(timeout);
diff --git a/react-native.web.js b/react-native.web.js
index 544cca82136ab14cbeec0a01499f554bd4eca75f..0097427b262f11fa3de27dff39d99adc8a789fbf 100644
--- a/react-native.web.js
+++ b/react-native.web.js
@@ -3954,12 +3954,17 @@ function setDidLayout(ctx) {
 // src/core/calculateItemsInView.ts
 var RENDER_RANGE_PROJECTION_FULL_VELOCITY = 4;
 var RENDER_RANGE_PROJECTION_SETTLE_DELAY = 100;
+// PATCH(foam): most of the trailing buffer the projection may consume.
+var RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT = 0.5;
 function getProjectedBufferAdjustment(scrollVelocity, trailingBuffer) {
   if (trailingBuffer <= 0) {
     return 0;
   }
   const velocityProgress = Math.min(1, Math.abs(scrollVelocity) / RENDER_RANGE_PROJECTION_FULL_VELOCITY);
-  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress;
+  // PATCH(foam): the caller adds this to BOTH buffered edges, so at full velocity the
+  // trailing edge lands on the viewport and rows behind the scroll leave the render
+  // range until the settle - chat scrollback shows holes where they should be (#858).
+  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress * RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT;
 }
 function scheduleRenderRangeProjectionSettle(ctx) {
   const state = ctx.state;
@@ -3973,7 +3978,10 @@ function scheduleRenderRangeProjectionSettle(ctx) {
     state.timeoutRenderRangeProjectionSettle = void 0;
     state.timeouts.delete(timeout);
     state.scrollHistory.length = 0;
-    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state);
+    // PATCH(foam): without this flag updateItemPositions starts at
+    // state.startBuffered, so a row above it keeps the stale position it got from
+    // the size estimate and leaves a hole below itself (#858).
+    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state, { forceFullItemPositions: true });
   }, RENDER_RANGE_PROJECTION_SETTLE_DELAY);
   state.timeoutRenderRangeProjectionSettle = timeout;
   state.timeouts.add(timeout);
diff --git a/react-native.web.mjs b/react-native.web.mjs
index 6b2d9011f6c5e85664eca195de538617fe7d9dee..f8036f1cf112457988612a33d640142c759b5acf 100644
--- a/react-native.web.mjs
+++ b/react-native.web.mjs
@@ -3933,12 +3933,17 @@ function setDidLayout(ctx) {
 // src/core/calculateItemsInView.ts
 var RENDER_RANGE_PROJECTION_FULL_VELOCITY = 4;
 var RENDER_RANGE_PROJECTION_SETTLE_DELAY = 100;
+// PATCH(foam): most of the trailing buffer the projection may consume.
+var RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT = 0.5;
 function getProjectedBufferAdjustment(scrollVelocity, trailingBuffer) {
   if (trailingBuffer <= 0) {
     return 0;
   }
   const velocityProgress = Math.min(1, Math.abs(scrollVelocity) / RENDER_RANGE_PROJECTION_FULL_VELOCITY);
-  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress;
+  // PATCH(foam): the caller adds this to BOTH buffered edges, so at full velocity the
+  // trailing edge lands on the viewport and rows behind the scroll leave the render
+  // range until the settle - chat scrollback shows holes where they should be (#858).
+  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress * RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT;
 }
 function scheduleRenderRangeProjectionSettle(ctx) {
   const state = ctx.state;
@@ -3952,7 +3957,10 @@ function scheduleRenderRangeProjectionSettle(ctx) {
     state.timeoutRenderRangeProjectionSettle = void 0;
     state.timeouts.delete(timeout);
     state.scrollHistory.length = 0;
-    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state);
+    // PATCH(foam): without this flag updateItemPositions starts at
+    // state.startBuffered, so a row above it keeps the stale position it got from
+    // the size estimate and leaves a hole below itself (#858).
+    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state, { forceFullItemPositions: true });
   }, RENDER_RANGE_PROJECTION_SETTLE_DELAY);
   state.timeoutRenderRangeProjectionSettle = timeout;
   state.timeouts.add(timeout);
diff --git a/react.js b/react.js
index 544cca82136ab14cbeec0a01499f554bd4eca75f..0097427b262f11fa3de27dff39d99adc8a789fbf 100644
--- a/react.js
+++ b/react.js
@@ -3954,12 +3954,17 @@ function setDidLayout(ctx) {
 // src/core/calculateItemsInView.ts
 var RENDER_RANGE_PROJECTION_FULL_VELOCITY = 4;
 var RENDER_RANGE_PROJECTION_SETTLE_DELAY = 100;
+// PATCH(foam): most of the trailing buffer the projection may consume.
+var RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT = 0.5;
 function getProjectedBufferAdjustment(scrollVelocity, trailingBuffer) {
   if (trailingBuffer <= 0) {
     return 0;
   }
   const velocityProgress = Math.min(1, Math.abs(scrollVelocity) / RENDER_RANGE_PROJECTION_FULL_VELOCITY);
-  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress;
+  // PATCH(foam): the caller adds this to BOTH buffered edges, so at full velocity the
+  // trailing edge lands on the viewport and rows behind the scroll leave the render
+  // range until the settle - chat scrollback shows holes where they should be (#858).
+  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress * RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT;
 }
 function scheduleRenderRangeProjectionSettle(ctx) {
   const state = ctx.state;
@@ -3973,7 +3978,10 @@ function scheduleRenderRangeProjectionSettle(ctx) {
     state.timeoutRenderRangeProjectionSettle = void 0;
     state.timeouts.delete(timeout);
     state.scrollHistory.length = 0;
-    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state);
+    // PATCH(foam): without this flag updateItemPositions starts at
+    // state.startBuffered, so a row above it keeps the stale position it got from
+    // the size estimate and leaves a hole below itself (#858).
+    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state, { forceFullItemPositions: true });
   }, RENDER_RANGE_PROJECTION_SETTLE_DELAY);
   state.timeoutRenderRangeProjectionSettle = timeout;
   state.timeouts.add(timeout);
diff --git a/react.mjs b/react.mjs
index 6b2d9011f6c5e85664eca195de538617fe7d9dee..f8036f1cf112457988612a33d640142c759b5acf 100644
--- a/react.mjs
+++ b/react.mjs
@@ -3933,12 +3933,17 @@ function setDidLayout(ctx) {
 // src/core/calculateItemsInView.ts
 var RENDER_RANGE_PROJECTION_FULL_VELOCITY = 4;
 var RENDER_RANGE_PROJECTION_SETTLE_DELAY = 100;
+// PATCH(foam): most of the trailing buffer the projection may consume.
+var RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT = 0.5;
 function getProjectedBufferAdjustment(scrollVelocity, trailingBuffer) {
   if (trailingBuffer <= 0) {
     return 0;
   }
   const velocityProgress = Math.min(1, Math.abs(scrollVelocity) / RENDER_RANGE_PROJECTION_FULL_VELOCITY);
-  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress;
+  // PATCH(foam): the caller adds this to BOTH buffered edges, so at full velocity the
+  // trailing edge lands on the viewport and rows behind the scroll leave the render
+  // range until the settle - chat scrollback shows holes where they should be (#858).
+  return Math.sign(scrollVelocity) * trailingBuffer * velocityProgress * RENDER_RANGE_PROJECTION_MAX_TRAILING_SHIFT;
 }
 function scheduleRenderRangeProjectionSettle(ctx) {
   const state = ctx.state;
@@ -3952,7 +3957,10 @@ function scheduleRenderRangeProjectionSettle(ctx) {
     state.timeoutRenderRangeProjectionSettle = void 0;
     state.timeouts.delete(timeout);
     state.scrollHistory.length = 0;
-    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state);
+    // PATCH(foam): without this flag updateItemPositions starts at
+    // state.startBuffered, so a row above it keeps the stale position it got from
+    // the size estimate and leaves a hole below itself (#858).
+    (_a3 = state.triggerCalculateItemsInView) == null ? void 0 : _a3.call(state, { forceFullItemPositions: true });
   }, RENDER_RANGE_PROJECTION_SETTLE_DELAY);
   state.timeoutRenderRangeProjectionSettle = timeout;
   state.timeouts.add(timeout);
